<Application x:Class="AcademicClaimHub.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="Views\MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>

            <!-- ================= Palette ================= -->
            <!-- Core colors for dark theme -->
            <SolidColorBrush x:Key="PrimaryBackground" Color="#1E1E2E"/>
            <SolidColorBrush x:Key="SecondaryBackground" Color="#2A2A3D"/>
            <SolidColorBrush x:Key="PrimaryText" Color="#E0E0E0"/>
            <SolidColorBrush x:Key="SecondaryText" Color="#C8C8C8"/>
            <SolidColorBrush x:Key="AccentColor" Color="#4FC3F7"/>
            <SolidColorBrush x:Key="DangerColor" Color="#FF5C5C"/>
            <SolidColorBrush x:Key="SuccessColor" Color="#7ED957"/>

            <!-- ================= Window Style ================= -->
            <!-- Default appearance for all windows -->
            <Style TargetType="Window">
                <Setter Property="Background" Value="{StaticResource PrimaryBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
            </Style>

            <!-- ================= TextBlock Style ================= -->
            <!-- Default text color for all TextBlocks -->
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            </Style>

            <!-- ================= Button Style ================= -->
            <!-- Standard button look: accent color, bold text, hand cursor -->
            <Style TargetType="Button">
                <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <!-- ================= TextBox Style ================= -->
            <!-- Dark-themed input boxes with padding and border -->
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="#262738"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                <Setter Property="BorderBrush" Value="#3A3B4F"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="6"/>
            </Style>

            <!-- ================= DataGrid Style ================= -->
            <!-- Dark-themed tables with fixed row height, read-only by default -->
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="#1E1E2E"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="RowHeight" Value="32"/>
                <Setter Property="GridLinesVisibility" Value="None"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="HorizontalGridLinesBrush" Value="#33364A"/>
                <Setter Property="VerticalGridLinesBrush" Value="#33364A"/>
                <Setter Property="ColumnHeaderHeight" Value="34"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="AutoGenerateColumns" Value="False"/>
            </Style>

            <!-- Column header appearance -->
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#2A2A3D"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Padding" Value="8,6"/>
            </Style>

            <!-- Row appearance with hover and selection highlights -->
            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="#252738"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#34364B"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#3F77A8"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
